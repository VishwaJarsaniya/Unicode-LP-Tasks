import React from "react";
import ReactDOM from "react-dom/client";
import LPApp from "./LPApp1";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <LPApp />
  </React.StrictMode>
);